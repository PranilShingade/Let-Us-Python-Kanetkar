# b) Which of the following are valid return statements? return (a, b, c) return a + b + c return a, b, c

# Output:
# All three return statements are valid in Python.
# Explanation:
# 1. return (a, b, c) - This returns a tuple containing the values of a, b, and c. It is a valid return statement.
# 2. return a + b + c - This returns the sum of a, b, and c. It is a valid return statement.
# 3. return a, b, c - This returns a tuple containing the values of a, b, and c. It is a valid return statement.
# In Python, you can return multiple values from a function using a tuple, and you can also return the sum of values directly. Therefore, all three return statements are valid.

# In summary, return (a, b, c) returns a tuple containing the values of a, b, and c.
# return a + b + c returns the sum of a, b, and c.
# return a, b, c returns a tuple containing the values of a, b, and c.
